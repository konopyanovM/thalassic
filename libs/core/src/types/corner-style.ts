/**
 * Corner curvature the design system draws on its controls: the continuous
 * squircle curve where the browser can draw it, or plain circular rounding.
 */
export type cornerStyle = 'squircle' | 'round';
