import { ThemeConfig } from '../types';

export const THEME_CONFIG_DEFAULT: ThemeConfig = {
  darkThemeClass: 'tls-dark',
  lightThemeClass: 'tls-light',
  defaultTheme: 'system',
  localStorageKey: 'tls-theme',
};
