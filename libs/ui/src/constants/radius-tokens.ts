import { radius } from '../types';

/** Every step of the theme radius scale, used to tell a token apart from a raw CSS length. */
export const RADIUS_TOKENS: readonly radius[] = ['sm', 'md', 'lg', 'xl', '2xl', '3xl'];
