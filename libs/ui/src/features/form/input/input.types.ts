/**
 * How the field presents itself at rest. `outlined` is the bordered field;
 * `plain` is dressed as the text around it — no border or surface until it is
 * hovered or focused — for a value that reads as a title or a label of its
 * own, edited in place.
 */
export type inputVariant = 'outlined' | 'plain';
