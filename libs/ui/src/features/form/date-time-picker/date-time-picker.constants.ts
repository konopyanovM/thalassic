/** `date-fns` format token for the trigger label in `calendar` mode, e.g. "Jul 20, 2026". */
export const DATE_LABEL_FORMAT = 'MMM d, yyyy';

/** `date-fns` format token for the trigger label in `month-picker` mode, e.g. "July 2026". */
export const MONTH_LABEL_FORMAT = 'MMMM yyyy';

/** `date-fns` format token for the trigger label in `year-picker` mode, e.g. "2026". */
export const YEAR_LABEL_FORMAT = 'yyyy';

/** `date-fns` format token for a 12-hour time, e.g. "9:30 AM". */
export const TIME_LABEL_FORMAT_12 = 'h:mm a';

/** `date-fns` format token for a 24-hour time, e.g. "09:30". */
export const TIME_LABEL_FORMAT_24 = 'HH:mm';

/** `date-fns` format token for the day-period segment of the time picker, e.g. "AM". */
export const DAY_PERIOD_FORMAT = 'a';

/** Hours in a half-day, the wrap point of the 12-hour clock. */
export const HOURS_PER_HALF_DAY = 12;
