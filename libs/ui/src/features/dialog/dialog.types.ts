export type dialogSize = 'sm' | 'md' | 'lg' | 'xl' | 'wide' | 'full';

/**
 * Horizontal alignment of a dialog footer's action buttons, mapped onto
 * `justify-content` (direction-aware: `start`/`end` follow the layout
 * direction). Mirrors `confirmActionsAlign`.
 */
export type dialogFooterAlign = 'start' | 'center' | 'end' | 'space-between';
